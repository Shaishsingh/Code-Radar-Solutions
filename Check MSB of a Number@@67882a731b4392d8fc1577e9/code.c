#include<stdio.h>
int main(){
    int num , result;
    scanf("%d%d",&num);
    result = right << 1;
    if(result && 1)
    { printf("Set"); }
    else 
    {printf("Not Set");}
    return 0;
}